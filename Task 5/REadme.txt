I launched Wireshark in my VMware environment, selected the eth0 Ethernet interface, and began capturing network traffic. Then, I accessed the testfire.net website through a browser and analyzed the resulting packets—including TCP, HTTP, and DNS protocols. Finally, I exported the captured data into PCAP files, which are included in the folder for reference.

files:-
dns.pcap
http.pcap
tcp.pcap